# Arthur Windows admin bootstrap repair

This branch repairs the persistent Supervisor helper syntax gate before any administrator-level Windows bootstrap is attempted. It does not build firmware, create a Candidate, flash a device, or modify durable runtime state.
