name: Arthur Repair Task Tests

'on':
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  repair-task-contract:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Arthur Windows repair task contract
        shell: pwsh
        run: ./tests/arthur-windows-repair-task.tests.ps1
