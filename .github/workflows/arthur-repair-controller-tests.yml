name: Arthur Repair Controller Tests

'on':
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  diagnostic-contract:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Arthur Windows repair controller diagnostic contract
        shell: pwsh
        run: ./tests/arthur-windows-repair-controller.tests.ps1
