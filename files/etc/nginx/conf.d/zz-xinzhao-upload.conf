# XinZhaoWrt Arthur large-upload safety.
# Nginx request bodies must not consume /tmp tmpfs/RAM while LuCI receives
# sysupgrade images. The final LuCI handoff intentionally remains
# /tmp/firmware.bin for standard sysupgrade RAM-root semantics.
client_body_temp_path /root/.xinzhao-upload/nginx 1 2;
